## Angular Starter Pack w/ gulp

This project is mostly useless I just wanna experience making my own gulp build pipeline
from scratch

## How to Run
1. Clone Repo
2. cd into repo
3. run `npm install` and `bower install`
4. No server yet.